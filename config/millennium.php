<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Contraseña demo local (LocalLoginCredentialsSeeder)
    |--------------------------------------------------------------------------
    |
    | Si está definida y no vacía, se aplica a todos los correos del seeder.
    | Definila solo en .env local; no la subas en repos públicos.
    |
    */

    'local_demo_password' => env('MILLENNIUM_LOCAL_DEMO_PASSWORD', ''),

];
